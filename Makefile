.PHONY: clean kernel bootloader sys drivers all merge

kernel:
	@echo "wip"

bootloader:
	@echo "wip"

sys:
	@echo "wip"

drivers:
	@echo "wip"

all:
	@echo "wip"

merge:
	@echo "wip"

clean:
	@find . -type f -name '*.bin' -delete
